# DeepSeek Harness Desktop v2.1.0

本次版本新增“模型资源中心”，重点保证资源信息可见，同时不阻塞对话流程。

## 新增

- 顶部状态胶囊显示当前会话模型、账户额度或今日本机 Token 用量。
- 模型资源中心区分当前路由、账户共享额度、扩展积分和本机可观察用量。
- 支持 ChatGPT/Codex 五小时与周额度窗口、余额/积分及重置时间的数据结构。
- 一键打开官方 Codex 用量页面、重新登录或后台刷新。

## 使用体验

- 窗口优先读取无凭据的本地缓存，立即渲染。
- 压缩会话解析在 Worker 中执行，不占用 Electron 主线程。
- 账户探测最多等待 5 秒，失败时保留缓存和本机用量，不阻塞窗口。
- 会话变化自动触发防抖后台更新，不进行高频轮询。

## 隐私与数据边界

- OAuth Token、账户 ID 和凭据路径不会发送给 Renderer，也不会写入资源缓存。
- 本机 Token 账本明确标注为“本机可观察范围”，不冒充账户总量。
- 未知数据展示为“暂不可读”，不会显示成虚假的零余额。
