# Harness Desktop 项目规则

- 实施前确认工作区、源码版本与实际安装位置，保留已有未提交改动。按本次变更选择最小充分验证，不默认全量重建或重装。
- 修改正在使用的 Profile、preset、provider 或运行时文件前，评估热重载、现有任务和重启边界。影响活动任务的切换应安排在已确认的安全窗口；不能把“支持热重载”当作对所有服务和任务均无影响。
- 升级和回滚沿用 [发布更新流程](docs/release-updates.md)：隔离准备与验证、在安全窗口切换、保留可用回滚及数据兼容边界。记录本次源码、候选包与安装态对应关系，不在本文件写死版本或端口。
- 配置包含某服务、进程存活或端口监听仅是局部证据。诊断核对当前实例的端口、认证、日志和服务状态；修复会话发送或模型切换后，验证对应实际操作/RPC，优先使用隔离测试会话。未做端到端验证时明确说明。
- 源码、单测或候选包通过不代表安装版已生效。需要交付安装版时核对安装记录、包标识及启动状态，再验证用户遇到的故障；重启恢复与根因修复分开报告。
- 清理按 [存储维护规则](docs/storage-maintenance.md) 预览并核对准确目标、体积和保留范围。默认仅处理可重建发布/安装产物，不把微信/企业微信聊天、业务图片、会话历史、任务清单或已验收成品当缓存删除。
- 推送、发布或更新 README 附件前核对当前目录、remote URL、目标分支和附件归属。Desktop 的说明、截图、安装包属于 Desktop 仓库；上游贡献进入对应 Harness fork/贡献分支，不能仅凭相似目录名确定目标。发布权限以本次任务授权为准。
