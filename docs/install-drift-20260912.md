# 安装目录漂移与切换边界 · 2026-09-12

## 已完成：迁移与 r9 激活

用户批准后，以 76 项精确清单（69 业务、5 企业微信、2 维护备份，479,008 bytes）执行了快照、逐叶迁出与可信安装登记恢复。原字节/计划/日志保存在 `C:/Users/yi/.dsh/backups/install-recovery-20260912`；业务目录为 `C:/Users/yi/Documents/DeepSeek/recovered-install-workspace-20260912`。22 个 Python 仅修正旧安装根路径，语法通过；另外54项保持原字节。企业微信仅新增明确 configDir，原全局另一套凭据及 disabled wrapper 不变。

r9 已于 2026-09-12 18:14 激活，18:15 启动并验收；默认引擎 cwd 已改为现有 `Documents/DeepSeek`，85 个工作区会话及索引/置顶未变。当前包 SHA256 `a5d2d3f7dce55677e3ce375ea62444901e41e90c4de3ed9f476f6bccda44e045`，启动后完整28769文件校验通过。旧程序备份 `DeepSeek.pre-update-20260912-181421`。完整证据、边界及遗留见 `business-upgrade-status-20260911.md`，不要重新执行已完成的迁移。

## 迁移前审计（历史记录）

以下为初审的58项及当时尚未获准的拟议方案，仅供追溯；后来已由上述76项清单和用户授权替代。

- 当前程序仍为 r7；`desktop-release.json` 哈希仍为 `4e6d46e540855ebf6a0335d4f132c7f2980da6d981f66277681e2246198a41ca`，但这不代表目录内容仍通过验证。
- 原清单 28,767 项，实际 28,825 项；新增 58 项，无缺失。额外文件仅做路径/类型分类，没有读取业务正文或凭据值。
- 对原清单白名单逐项比对，仅 `resources/runtime/node_modules/@deepseek-ai/dsh-host-webserver/lib/index.js` 内容变化。旧 SHA256 `040500dc252b5c0f1e8f577af873e1ac4a38a554d2e6dc2f8c5a4c8d2245be4d`，当前 `7e0043e750cc61cf886414be96e5f51ce4aa883f6be3ca1f17fbb2607861a5ce`。
- 变化是既有 `createServer({ maxHeaderSize: 131072 }, ...)` 热修。注释记录其用于缓解随机端口认证 Cookie 积累导致的 431 / 插件脚本加载失败。候选保留相同行为，不宣称此次根治 Cookie 积累。
- Desktop 的引擎启动 `cwd` 指向可执行文件目录；这是业务文件混入安装目录的确定风险，但没有据此认定每个额外文件的写入者。
- 发布验证已正确拒绝当前混合目录。本轮未绕过、重封或修改原安装清单，未执行 `PrepareOnly` 或激活。

## 额外资料及拟处理方案（尚未执行）

| 范围 | 类型与依赖 | 拟处理 |
| --- | --- | --- |
| 安装根目录的 Python、JSON、TXT、PNG | 业务脚本、计算输出与截图；可能存在相对路径和原会话工作区依赖 | 安全停止使用者后，迁入独立业务工作区并保持相对结构；核对引用后再切换应用。 |
| 安装根目录 `.dsh/wecom-cli` | 企业微信本地加密凭据及缓存；不能当发布缓存清理 | 与对应业务工作区一同保持目录关系，先核对 CLI 查找基准；不输出、不复制到发布包，也不重新登录覆盖现有凭据。 |
| runtime 的 `dsh-settings/lib` 两份备份 | 历史修补备份，当前有效模块本身与 r7 原清单一致 | 经核对后保留到单独维护备份，不混入新版 runtime。 |

建议的新业务目录为 `C:/Users/yi/Documents/DeepSeek/recovered-install-workspace-20260912`，维护备份使用独立归档。**这些是目的地建议，不表示已迁移或已获准调整业务工作区。** 修改默认工作目录、历史工作区引用和企业微信查找路径均须明确方案后实施，不能仅移动文件就声称任务可续跑。

真正切换会将整个旧安装移为回滚目录；若不先处理资料依赖，原会话/企业微信可能找不到文件。需要用户对具体资料保留与工作区迁移方案授权，并在所有相关任务安全停下后执行。无需关闭安全校验或提高系统权限。

## 可继续的部分

已验证封存 r7 成品仍完整（28,767 项），可用它准备独立干净 runtime；只重放上述已审热修，不带入额外业务或凭据文件。新技能、离线预览、源码 CLI 和候选隔离测试可继续；它们不表示正在运行的桌面已升级。
