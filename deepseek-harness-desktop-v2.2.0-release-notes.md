# DeepSeek Harness Desktop v2.2.0

本次版本加入低开销的原生会话置顶，并完善模型资源中心与中文项目说明。

## 新增：原生会话置顶

- 会话操作菜单新增“置顶会话 / 取消置顶”。
- 当前空白新会话保持首位，置顶会话排列在普通会话之前。
- 工作区视图与单列表视图均支持置顶，重启后状态保留。
- 排序直接集成到 React 会话数据层，没有 DOM 轮询、`MutationObserver`、文件扫描或额外后台进程。
- 最多保存 50 个置顶会话 ID，避免本地状态无界增长。

## 模型资源中心

- 顶部显示当前模型，并在提供商允许时展示用量、余额与重置时间。
- 账户服务不可用时降级为本机会话 Token 统计。
- 账户探测与本地统计在后台执行，不阻塞主界面操作。

## 兼容性与性能

- 保留搜索、重命名、分叉、归档、工作区分组和拖拽排序流程。
- 上游客户端结构变化时，构建阶段会拒绝应用不完整补丁。
- 新增稳定排序、幂等补丁和热路径禁用项测试。
- 2000 条可见会话的排序测试低于 5 ms 性能预算。

## 下载

- `DeepSeek-Setup-2.2.0.exe`：Windows 安装版
- `DeepSeek-Desktop-2.2.0-portable.exe`：Windows 便携版

> 本项目是社区维护的非官方桌面封装，不代表 DeepSeek 官方立场。
